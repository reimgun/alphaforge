ccxt==4.3.89
pandas==2.2.2
numpy==1.26.4
python-dotenv==1.0.1
requests==2.31.0
rich==13.7.1
scikit-learn==1.4.2
xgboost==2.0.3
# LightGBM (optional, ML_MODEL_TYPE=lightgbm oder FEATURE_LIGHTGBM=true):
# pip install lightgbm>=4.3.0
joblib==1.4.0
anthropic==0.25.0

# ── LLM Providers (optional — nur den installierten Provider) ─────────────────
# AI_PROVIDER=openai oder groq:
openai>=1.30.0
# AI_PROVIDER=gemini:
# pip install google-generativeai>=0.7.0
# AI_PROVIDER=ollama: kein Paket nötig (nutzt requests, bereits installiert)

# Dashboard
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
streamlit>=1.37.0
streamlit-autorefresh>=1.0.1
plotly>=5.20.0
httpx>=0.27.0

# WebSocket
websockets>=12.0

# LSTM (CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu)
torch>=2.2.0

# Encrypted Config (optional)
cryptography>=42.0.0

# OHLCV Cache (Parquet-Format)
pyarrow>=15.0.0

# PDF Reports
fpdf2>=2.7.0

# Scipy (Platt Calibration, Bayesian Confidence Intervals)
scipy>=1.12.0

# Testing
pytest>=8.1.0
pytest-cov>=5.0.0
